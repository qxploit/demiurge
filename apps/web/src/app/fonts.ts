import { Cinzel, Cinzel_Decorative } from "next/font/google";

export const cinzel = Cinzel({ subsets: ["latin"], weight: ["600", "700", "900"] });
export const cinzelDecorative = Cinzel_Decorative({ subsets: ["latin"], weight: ["700", "900"] });
